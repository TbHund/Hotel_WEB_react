import React from 'react';
import { Container, Typography, Box } from '@mui/material';

const AdminPage = () => {
    return (
        <Container>
            <Typography variant="h4">Административная панель</Typography>
            <Box>Здесь будут административные функции</Box>
        </Container>
    );
};

export default AdminPage;